{
  "name": "bunwalker",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Rick Vause",
  "license": "MIT",
  "dependencies": {
    "picnic": "^6.5.5"
  },
  "devDependencies": {
    "@babel/core": "^7.14.5",
    "@babel/preset-env": "^7.14.5",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.34.1"
  },
  "scripts": {
    "dev": "parcel serve src/index.html -d build/dist --cache-dir build/cache --host dev.arvors.pl --port 8888 --no-source-maps",
    "build": "parcel build src/index.html -d build/dist --cache-dir build/cache --no-source-maps"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
